using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Leaf node beavior used to check if the player is in LOS
/// </summary>
public class FindPlayer : Behavior
{
    // Start is called before the first frame update
    public FindPlayer()
    {
        Debug.Log("Created FindPlayer behavior ");
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
